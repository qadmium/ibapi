<Window x:Class="Sample.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShellView" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Name="PlaceOrder" Margin="5">Place order</Button>
            <Button Name="SymbolSearch" Margin="0,5,5,5">Symbol search</Button>
        </StackPanel>
        <TabControl Name="Items" Grid.Row="1"></TabControl>
        <StatusBar Grid.Row="2">
        </StatusBar>
    </Grid>
</Window>
